apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: omnigent-postgres
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: omnigent-postgres
    component: postgres
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
